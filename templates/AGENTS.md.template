# <專案名> — Agent 入口

<一句話說明產品、主要技術棧與 source roots。>

## Current truth

- 產品現行規格：<路徑>
- 專案狀態與目前 slice：<路徑>
- 每個 module 的責任與公開介面：讀該 module root 的 `architecture.md`。
- 歷史與被取代決策不得混入現行規格；需要歷史時才查版本紀錄。

## Intent routing

- 簡略產品想法、新系統或未定義的新行為：讀 `.agents/skills/spec-driven-development/SKILL.md`，只完成 DEFINE。
- 已有接受且 READY 的單一 module 規格，需要設計架構或 slices：讀 `.agents/skills/planning-and-task-breakdown/SKILL.md`，只規劃目前 stage。
- 已有完整 Task Packet 的實作：讀 `.agents/skills/incremental-implementation/SKILL.md`；必要新測試由隔離角色另讀 `test-driven-development`。
- 已有可重現 code failure：讀 `.agents/skills/debugging-and-error-recovery/SKILL.md`，依 Red → Yellow → Green fallback 處理。
- 長 session、交接、上下文或協調成本問題：讀 `.agents/skills/lean-development/SKILL.md`；它不取代目前 phase owner。
- 不要在 session 啟動時預載所有 skills。只載入目前 intent owner，以及任務確實需要的單一跨切面 owner。

## Scope and evidence

- 執行者只能修改 Task Packet 的 `write_paths`，不得修改 `architecture.md`、test-owned paths 或其他 module。
- 跨 module 變更先停止 BUILD，交由架構負責人更新共通規格與 module contracts。
- Agent 只判斷 deterministic code evidence；視覺、互動手感與易用性保留給人類接受。
- 驗證由 Red → Yellow → Green fallback 擴張，不以全量測試作為例行收尾。
- <專案特有的保護路徑、建置入口、發布權限與外部副作用限制。>

## Environment facts

<只寫已實測的 OS、工具、建置與局部驗證入口；不把推測寫成事實。>
